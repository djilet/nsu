import 'package:flutter/material.dart';

class DeanofficeScreen extends StatelessWidget {
  const DeanofficeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('EDeanofficeScreen'),
      ),
    );
  }
}
